{% extends 'price_scrape/basePS.html.twig' %}

{% block title %}Price Scrape Tool{% endblock %}

{% block content %}
<div class="card mb-3">
    <div class="card-body">
    <form method="POST" action="">
        <div class="form-group">
            <div class="form-row">
                <div class="col-md-12">
                    <label for="deviceName">Device</label>
                    <input class="form-control" id="deviceName" type="text" placeholder="Enter complete device name">
                    <div id="response"></div>
                </div>
            </div>
        </div>
        <button class="btn btn-primary btn-lg btn-block" onClick="addDevice()">Add</button>
    </form>
    </div>
</div>
{% endblock %}

{% block js %}
<script>
    function addDevice(){
        $device = document.getElementById('deviceName').value;
        var xhttp = new XMLHttpRequest();
        xhttp.onreadystatechange = function(){
            if(this.readyState == 4 && this.status == 200){
                var time = new Date() - start;
                document.getElementById('response').innerHTML = this.responseText;
            }
        };
        xhttp.open("POST", "add-device.php", true);
        xhttp.setRequestHeader("Content-type", "application/x-www-form-urlencoded");
        xhttp.send("deviceName=" + $device);
    }
</script>
{% endblock %}